name: Release Please

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release_please:
    name: placeholder
    runs-on: ubuntu-latest
    steps:
      - name: placeholder
        shell: bash
        run: |
          date
